# Screenshot Placeholders

This folder reserves stable paths for future screenshots.

Planned screenshots:

- `cli-help.svg`
- `import-progress.svg`
- `search-results.svg`
- `doctor.svg`

The current placeholders are intentionally lightweight so the README can link to stable assets before polished screenshots are captured.
